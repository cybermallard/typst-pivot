// Visual-regression: the rendered example is the reference. Editing the example
// (or the renderer) must be confirmed by re-approving the reference. Anchors the
// straight long edge (Block & alert -> Report) dropping into the widened merge.
#include "/examples/flowchart-triage.typ"
