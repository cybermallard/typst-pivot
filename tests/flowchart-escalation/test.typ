// Visual-regression: the rendered example is the reference. Editing the example
// (or the renderer) must be confirmed by re-approving the reference. Anchors the
// left-to-right routing a rightward flow transposes: a forking decision (right-angle
// exits from the diamond's cross points), a single-onward decision (straight
// through), and a back edge looping over the body into "Triage".
#include "/examples/flowchart-escalation.typ"
