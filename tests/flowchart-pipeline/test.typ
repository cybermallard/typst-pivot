// Visual-regression: the rendered example is the reference. Editing the example
// (or the renderer) must be confirmed by re-approving the reference. Anchors a
// left-to-right flow (orientation: "horizontal") — the whole layout transposed, with a
// branch, a merge, and the flow-perpendicular widening running vertically.
#include "/examples/flowchart-pipeline.typ"
